export interface RequestProxy {
    proxy: {
        protocol: string;
        hostname: string;
        port: number;
        auth: {
            username: string;
            password: string;
        };
    };
}
export declare const configureProxy: (requestURL: string) => RequestProxy;
